# Third-Party Notices

This project itself is released under [The Unlicense](../LICENSE).
It bundles or invokes the following third-party software, each under its own license.

## npm dependencies

Full list with versions: run `npm ls --all` — don't hand-maintain a duplicate of `package.json`/`package-lock.json` here.
Every dependency listed below is verified directly against each package's own `package.json` `license` field, not assumed; none of it is copyleft.

Direct runtime/peer dependencies — listed even though none carry an attribution requirement, so a reader doesn't have to run the tool just to see there's nothing unusual here:

| Package                | License    | Note                                    |
| ---------------------- | ---------- | --------------------------------------- |
| `@pimbay/search-query` | Unlicense  | Runtime dependency — required directly. |
| `drizzle-orm`          | Apache-2.0 | Peer dependency — required directly.    |

## Notes

This is not legal advice.
